// Central configuration for the application environment and api endpoints.
export const config = {
  port: 3000,
  stocksApiUrl: "https://keligmartin.github.io/api/stocks.json",
  requestTimeoutMs: 10000
};